<h1 class="pageTitle">{{recipe.title}}</h1>
<div id="main">
    <div>
        <ul>
            <li><p>{{recipe.description}}</p></li>
            <li><p>{{recipe.ingredients}}</p></li>
            <li><p>{{recipe.instructions}}</p></li>
        </ul>

        {{#if error}}
            <p class="errorMessage">{{error}}</p>
        {{/if}}
    </div>
</div>