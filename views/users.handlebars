<div id="main">
    <h1>Users</h1>
    <div class="textCard">
        <ul>
            {{#each users}}
                <li>
                    {{username}} - {{password}}
                </li|>
            {{/each}}
        </ul>
    </div>
</div>